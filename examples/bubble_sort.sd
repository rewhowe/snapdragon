※
　v1.1.0（未定、未確認）
※
数値列を バブルソートするとは
	もし 数値列の 長さが 1以下 ならば
		数値列を 返す

	繰り返す
		入れ替えたかは いいえ

		1から 数値列の 長さまで 繰り返す
			今の指数は それ

			それから 1を 引く
			前の指数は それ

			もし 数値列の 前の指数が 数値列の 今の指数より 大きければ
				仮数は 数値列の 今の指数
				数値列の 今の指数は 数値列の 前の指数
				数値列の 前の指数は 仮数
				入れ替えたかは はい

		もし 入れ替えたか？ でなければ
			終わり

数値列は 6,7,8,9,10,1,2,3,4,5
数値列を バブルソートして
それを 表示する
