※ https://codegolf.stackexchange.com/questions/59014/generate-monday-numbers

(
月曜日数値Ｎの条件：
1．Ｎの十進表示に「0」が入ってない
2．Ｎに同じ数字が2個以上入ってない
3．Ｎを各桁で割れる
)
最大月曜日数値は 9867312

数値で 月曜日数値かを判定するとは
	重複確認用配列は 連想配列

	「【数値】」に 対して 繰り返す
		それを 数値化して
		桁は それ

		もし 桁が 0 であり、
		又は 重複確認用配列の 桁が あり、
		又は 数値を 桁で 割った余りを求める？ ならば
			いいえと なる

		重複確認用配列の 桁は 肯定

	はいと なる

※ 0から 最大月曜日数値まで 繰り返す
（まあ、出来るけど凄い時間かかるので、250までｗ）
0から 250まで 繰り返す
	今の数値は それ
	それで 月曜日数値かを判定して
	もし それが 肯定 ならば
		今の数値を 表示する
