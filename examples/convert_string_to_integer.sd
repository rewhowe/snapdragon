※ (Excludes use of built-in 整数化)
一桁を 文字化するとは
	数値は 0

	「0123456789」に 対して 繰り返す
		もし 一桁が 数値と 等しければ
			終わり
		数値に 1を 足して
		数値は それ

	それを 返す

数値を カスタム＿整数化するとは
	数値を 1で 割った余りを求めて
	小数は それ
	数値から 小数を 引いて
	返す

数値を 文字列化するとは
	結果文字列は 「」

	繰り返す
		数値を 10で 割った余りを求めて
		それを 文字化して
		それに 結果文字列を 繋いで
		結果文字列は それ

		数値を 10で 割って
		それを カスタム＿整数化して
		数値は それ

		もし 数値が 1より 低ければ
			終わり

	結果文字列と なる

0を 文字列化して
それを 言う ※「言う」の引数は文字列のみ
「\n」と 言う

42を 文字列化して
それを 言う
「\n」と 言う

46490000を 文字列化して
それを 言う
「\n」と 言う
