※ v1.0.0
数値列で 最低最大を見つけるとは
	もし 数値列の 長さが 0 ならば
		配列を 返す

	最低値は 数値列の 先頭
	最大値は それ

	数値列に 対して 繰り返す
		今の数値は それ

		もし 今の数値が 最低値より 低ければ
			最低値は 今の数値
		または 今の数値が 最大値より 高ければ
			最大値は 今の数値

	結果は 最低値、最大値
	返す


数値列は 配列
「空の配列\n」と 言う
数値列で 最低最大を見つける
「最低値：【それの 先頭】\n
	最大値：【それの 末尾】\n」と 言う

「\n1 から 10 の配列\n」と 言う
数値列は 5,4,3,2,1,10,9,8,7,6
数値列で 最低最大を見つける
「最低値：【それの 先頭】\n
	最大値：【それの 末尾】\n」と 言う
