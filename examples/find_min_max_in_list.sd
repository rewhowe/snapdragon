※
　v1.0.0
※
数値列で 最低最大を見つけるとは
	もし 数値列の 長さが 0 ならば
		戻る

	数値列のコピーは 数値列
	数値列のコピーから 先頭を抜き出す
	最低値は それ
	最大値は それ

	数値列に 対して 繰り返す
		今の数値は それ

		もし 今の数値が 最低値より 低ければ
			最低値は 今の数値
		または 今の数値が 最大値より 高ければ
			最大値は 今の数値

	結果は 最低値、最大値
	返す

結果列で 答えてみるとは
	「最低値：」と 言う
	結果列から 先頭を抜き出して
	それを 表示する

	「最大値：」と 言う
	結果列から 抜き出して
	それを 表示する

数値列は 配列
「空の配列：\n」と 言う
数値列で 最低最大を見つける
それで 答えてみる

「\n1 から 10 の配列：\n」と 言う
数値列は 5,4,3,2,1,10,9,8,7,6
数値列で 最低最大を見つける
それで 答えてみる
