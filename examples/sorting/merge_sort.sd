左と 右を マージするとは
	結果列は 配列

	繰り返す
		もし 左が 空 ならば
			終わり
		または 右が 空 ならば
			終わり

		もし 左の 先頭が 右の 先頭より 低ければ
			左から 先頭を引き出す
			結果列に それを 押し込む
		違えば
			右から 先頭を引き出す
			結果列に それを 押し込む

	もし 左の 長さが 1以上 ならば
		結果列に 左を 繋ぐ
		結果列は それ
	もしくは 右の 長さが 1以上 ならば
		結果列に 右を 繋ぐ
		結果列は それ

	結果列を 返す

数値列を マージソートするとは
	もし 数値列の 長さが 1以下 ならば
		数値列を 返す

	左は 配列
	右は 配列

	数値列の 長さを 2で 割って
	真ん中は それ
	1から 真ん中まで 繰り返す
		数値列から 先頭を引き出す
		左に それを 押し込む

	右は 数値列

	左を マージソートして
	左は それ

	右を マージソートして
	右は それ

	左と 右を マージして
	返す

数値列は 6,7,8,9,10,1,2,3,4,5
数値列を マージソートして
それを 表示する
