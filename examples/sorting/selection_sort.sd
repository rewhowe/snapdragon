数値列を セレクトソートするとは
	整理した配列は 配列

	繰り返す
		もし 数値列が 空 ならば
			整理した配列を 返す

		最低値は 数値列の 先頭

		数値列に 対して 繰り返す
			今の数は それ
			もし 今の数が 最低値より 小さければ
				最低値は 今の数

		整理した配列に 最低値を 押し込む
		数値列から 最低値を 抜く

数値列は 6,7,8,9,10,1,2,3,4,5
数値列を セレクトソートして
それを 表示する
